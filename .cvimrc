" Erik Westrup's cVim configuration.

" Modeline {{
"	 vi: foldmarker={{,}} foldmethod=marker foldlevel=0: tabstop=8: filetype=vim:
" }}

" General {{
	let barposition = "bottom"
	set noautofocus	" Prevent pages from stealing the cursor on page load.
" }}

set localconfig " Update settings via a local file (and the `:source` command) rather " than the default options page in chrome)0.
