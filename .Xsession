#!/usr/bin/env sh
# Initialization of X. Called by display managers.

# Unify .Xsession and .xinitrc
[ -x "${XDG_CONFIG_HOME:-$HOME/.config}/X11/xinitrc" ] && exec "${XDG_CONFIG_HOME:-$HOME/.config}/X11/xinitrc"
