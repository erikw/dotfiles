# Loadkeys keymap.
# Load this file like: $ sudo loadkeys -q $XDG_CONFIG_HOME/loadkeys/keymap
# get keycodes with `dumpkeys | less` or `showkey`.

# Translate Caps Lock key to Escape.
keycode 58 = Escape
# Put Caps Lock on Pause key.
keycode 119 = Caps_Lock

# Compose key on left super key.
keycode 125 = Compose
