# Pip packages to be installed globally.
# NOTE codified to ~/bin/glob_pkg_install_pip.sh
# NOTE asdf will use ./asdf-default-python-packages.txt instead

# Development
autoflake	# Fix flake errors directly.
autoimport	# Automatically import on use.
black		# (auto)formats Python code to uniform style.
flake8		# PEP8 style checker.
ipdb		# Improvement to pdb
ipython		# Improved REPL
pip-autoremove
pudb		# TUI debugger


# Additional
#goobook
