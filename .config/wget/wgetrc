# Configuration for wget.

# Don't obey robots.txt
robots = off

# Be XDG compliant. Can't use envvars in this config file
# Reference: https://www.gnu.org/software/wget/manual/html_node/Sample-Wgetrc.html
# wget is not creating director if not existing, thus not ~/.cache/wget/hsts
hsts-file = ~/.cache/wget-hsts
