" Adapt to Prettier style
setlocal expandtab			" Indent with spaces.
setlocal tabstop=2			" Let a tab be x spaces wide.
setlocal shiftwidth=2			" Tab width for auto indent and >> shifting.
setlocal softtabstop=2			" Number of spaces to act on for editing actions like BS and tab.
setlocal smarttab			" Indent with spaces at beginning of line.
setlocal shiftround			" Round indentation to nearest multiple of shiftwidth with <,>.
