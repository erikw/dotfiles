#!/usr/bin/env zsh
# Sleep specified time and then shut down
# Usage: sleep_shutdown 3h
# Usage: sleep_shutdown 2d

time="$1"
if has_command gsleep; then
	gsleep $time && shutdown -h now
else
	sleep $time && shutdown -h now
fi
