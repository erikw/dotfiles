# Global gradle properties
# Reference: https://docs.gradle.org/current/userguide/build_environment.html

# Build in parallel by default, for faster builds. Same as commandline --parallel.
org.gradle.parallel=true

# Bump default log level from lifecycle -> info.
#org.gradle.logging.level=info

# Speed up builds by caching outputs.
# Reference: https://docs.gradle.org/current/userguide/build_cache.html
#org.gradle.caching=true
