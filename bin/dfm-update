#!/usr/bin/env sh

set -e	# Exit on error

cd ${DFM_REPO:-$HOME/src/github.com/erikw/dotfiles}
git stash
git checkout personal
git fetch
git rebase origin/personal
git co local
git rebase personal
git stash pop || :
dfm install
